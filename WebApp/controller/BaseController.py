class BaseController:        var = None    async = False    status = 200        def __init__(self, var):        self.var = var    def __getattr__(self, key):        if key in self.var.keys():            return self.var[key]                    return None        def do(self, **argv):        pass        __call__ = do        def gerResult(self):        return ({}, self.status)